---
description: Code Style and Formatting Rules
globs:
alwaysApply: true
---

# Code Style and Formatting Rules

This project follows a strict set of code style and formatting guidelines to maintain readability and consistency. All new code should adhere to the patterns demonstrated in files such as `@file:src/utils/editorHelpers.ts`.

## TypeScript Guidelines

- Always use TypeScript strict mode
- Prefer `const` over `let`, avoid `var` completely
- Use async/await instead of promises when possible
- Avoid try-catch blocks unless absolutely necessary for error handling
- Write self-documenting code with clear variable names
- Keep functions small and focused on a single responsibility

## Naming Conventions

- Use camelCase for variables and functions
- Use PascalCase for classes and interfaces
- Use UPPER_CASE for constants
- Use kebab-case for file names
- Use descriptive names that explain intent

## Formatting

- Use 2 spaces for indentation
- Use single quotes for strings
- Add trailing commas in objects and arrays
- Use semicolons consistently
- Limit line length to 80 characters

## UI Components

- For UI elements, always prefer components from our shared MCP library. You should consult the available components at `@mcp<context7|resolve-library-uri>`.
- Avoid creating custom, one-off components when a suitable alternative exists in the shared library.

@kody-sync