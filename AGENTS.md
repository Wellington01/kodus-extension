# Agent conventions — kodus-extension

<!-- Kody rule-file detection test: root AGENTS.md (uppercase, no leading dot). -->

- Enforce the API conventions described in @docs/conventions.md on every changed file under `src/`.
- The convention identifier from that document MUST be quoted verbatim when flagging a violation.
